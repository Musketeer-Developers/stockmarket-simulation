apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmarket-simulation-deployment-brookline-bank
  namespace: ingress-nginx
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: stockmarket-simulation
        imagePullPolicy: Always
        image:  003718499156.dkr.ecr.us-east-1.amazonaws.com/stockmarket-simulation:brookline-bank
        ports:
          - containerPort: 3000
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: stockmarket-simulation-configmap-brookline-bank
              key: PORT
